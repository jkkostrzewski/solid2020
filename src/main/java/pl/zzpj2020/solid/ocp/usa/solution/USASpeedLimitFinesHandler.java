package pl.zzpj2020.solid.ocp.usa.solution;

public class USASpeedLimitFinesHandler {
    public double calculateSpeedLimitFine(USAState state, int speed) {
        double fine = 0.0;
        if (speed > state.getStateMaxSpeed()) {
            // calculate
        }
        return fine;
    }
}
